# DECISIONS - 核心架构决策记录

本文档记录 Mate Engine 项目的核心架构决策、技术选型规则、编码规范和发布规则。

---

## 1. 核心架构决策

### ADR-001: Unity 6 作为核心引擎
**状态**: Accepted  
**决策日期**: 2024-2025  
**决策内容**: 使用 Unity 6000.2.6f2 作为开发引擎

**理由**:
- 提供最新的 C# 语言特性支持
- 更好的性能优化和内存管理
- 对 VRM 和现代着色器支持更好
- 开发团队熟悉 Unity 工作流

**后果**:
- 需要开发者安装对应版本的 Unity
- 构建输出体积较大，但可接受

---

### ADR-002: 混合许可证模型
**状态**: Accepted  
**决策内容**: 项目采用混合许可证模型

- **主程序源码**: MateProv2 License (基于 AGPL v3 修改) + GNU AGPL v3
- **默认角色模型**: 保留所有权利 (Yorshka Shop)，禁止重新分发
- **集成组件**: 遵循各自原许可证（如 Qwen LLM 使用 Apache 2.0）

**理由**:
- 保护项目不被商业项目盗用
- 保持开源社区贡献开放
- 尊重原作者版权

**规则**:
- 禁止在商业平台（Steam、Epic、itch.io 等）分发未经授权的版本
- 修改分发必须公开完整源代码并使用相同许可证
- 模组（仅包含原创代码）可以在非商业前提下自由分发

---

### ADR-003: 支持 Linux 移植
**状态**: Accepted  
**当前仓库**: 这是 Linux 非官方移植版本  
**理由**:
- 原始项目主要针对 Windows
- 社区有 Linux 用户需求
- 通过 Unity 交叉编译工具链可以实现

---

### ADR-004: 内置 VRM 加载而非依赖 UPM
**状态**: Accepted  
**决策内容**: VRM 加载逻辑内置到项目中，不依赖外部 UPM 包

**理由**:
- 减少用户配置复杂度
- 更好的版本控制
- 针对 Mate Engine 用例做了定制优化

---

## 2. 技术选型规则

### 必须遵守
1. **着色器**: 使用 lilToon 或 Poiyomi Toon，不引入新的商业着色器
2. **VRM**: 保持自定义加载器，不改动核心 VRM 加载逻辑
3. **UI**: 使用 Unity UGUI，不引入新的 UI 框架
4. **动画**: 使用 Unity 原生 Animation + Timeline，不引入第三方动画系统
5. **AI**: 集成 LLM 使用 Apache 2.0 许可的开源模型，不强制要求闭源 API 密钥

### 禁止选择
- ❌ 不可引入闭源、商业授权的核心组件
- ❌ 不可引入会增加项目体积但实际使用率很低的依赖
- ❌ 不可改变许可证条款将项目转为商业闭源

---

## 3. 编码规范

### 命名规范
- **类名**: PascalCase
- **方法名**: PascalCase
- **变量/字段**: camelCase
- **常量**: ALL_CAPS_WITH_UNDERSCORES
- **文件名**: 和类名保持一致，PascalCase

### 文件组织
- 按功能模块组织到对应 `MATE ENGINE - XXX/` 目录
- 脚本资源放在 `MATE ENGINE - Scripts/` 下对应子目录
- 预制件和脚本放在同一目录层级
- `.meta` 文件必须提交到 Git

### 注释规范
- 公共 API 需要添加 XML 文档注释
- 复杂算法需要说明思路
- 保留 Unity 自动生成的 .meta 文件不修改

### 性能规范
- 避免在 Update 中进行 GC 分配
- 缓存组件引用，减少 GetComponent 调用
- 对常驻对象使用对象池
- 菜单栏和非频繁使用的代码可以放宽要求

---

## 4. 禁止修改的核心模块

以下模块是项目核心，修改前必须经过充分讨论和测试：

| 模块 | 路径 | 原因 |
|------|------|------|
| VRM 加载器 | `MATE ENGINE - Scripts/VRMLoader/` | VRM 格式兼容性敏感，修改容易导致破坏自定义角色加载 |
| 动画过渡系统 | `MATE ENGINE - Animations/` + 相关脚本 | 平滑动画过渡是项目核心卖点 |
| 许可版权信息 | 根目录 LICENSE.md, NOTICE.txt | 许可证法律文件，不得修改 |
| 项目文档 | README.md | 保持项目介绍完整性 |
| 系统托盘 | `MATE ENGINE - System Tray/` | Windows 平台特定代码，修改容易导致兼容性问题 |

---

## 5. 分支策略

- `main`: 主分支，稳定公开发布版本
- `X3.4-dev` 等: 开发分支，下一版本开发
- `feat-*`: 特性分支，单个功能开发（如当前 `feat-dev_talk`）
- `fix-*`: 修复分支，修复特定问题

---

## 6. 线上发布规则

### GitHub 发布流程
1. 在开发分支完成功能开发和测试
2. 合并到 main 分支
3. 打 Git 标签（如 `v3.3.0`）
4. 构建完整 ZIP 包
5. 创建 GitHub Release，附更新日志
6. 发布后公告社区

### Steam 发布流程
1. 由项目负责人处理
2. Steam 版本包含 Steam 创意工坊集成
3. Steam 发布版本与 GitHub 保持功能同步
4. 收入用于项目开发和 Steam 费用

### 发布检查清单
- [ ] 所有核心功能测试通过
- [ ] 更新版本号
- [ ] 更新更新日志
- [ ] 确认没有包含大文件（模型等）在 Git 中
- [ ] 确认 .gitignore 正确排除了 Unity 生成文件
- [ ] 确认许可证信息完整

### 发布版本号规范
- **格式**: `X{major}.{minor}.{patch}` (例如 `X3.3.0`)
- **major**: 大版本更新，核心架构变化
- **minor**: 新功能增加
- **patch**: Bug 修复，小改进

---

## 7. 模组开发规则

- 模组必须使用官方 Mod SDK 开发
- 模组不能修改 Mate Engine 核心代码
- 模组必须是非商业性质
- 包含源码修改的模组必须开源并使用相同许可证
- 禁止分发包含默认版权角色的衍生构建

---

## 8. 贡献规则

- 欢迎提交 Pull Request
- 提交前请自行测试编译
- 描述清楚修改内容和原因
- 大的新功能建议先开 Issue 讨论
- 所有贡献代码自动使用项目许可证发布

---

## 9. 决策记录历史

| 版本 | 日期 | 变更 |
|------|------|------|
| 1.0 | 2025-03-20 | 初始创建文档，记录核心架构决策 |
